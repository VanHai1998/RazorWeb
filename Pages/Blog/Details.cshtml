@page "/blog/chitietbaiviet/{id:int}"
@model ASP_RAZORWEB.Pages_Blog.DetailsModel

@{
    ViewData["Title"] = @Model.Article.Title;
}

<h1>@Model.Article.Title</h1>
<p>Ngày tạo: <i>@Html.DisplayFor(model => model.Article.Created)</i></p>
<hr/>
@* @Html.DisplayFor(model => model.Article.Content) *@
@Html.Raw(@Model.Article.Content);

<hr/>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Article?.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
